{#
/**
 * Block Name: Support CTA
 */
#}

{% include '/blocks/partial/standout.twig' with {
  content: {
    image: fields.image,
    title: fields.title,
    copy: fields.copy,
    buttonLabel: fields.button.title,
    buttonLink: fields.button.url,
    buttonClass: 'a-btn',
    extraCopy: fields.extra_copy
  },
  classes: [
    fields.add_image_overlay ? 'a-overlay' : null,
    fields.branding ? 'brand--' ~ fields.branding|lower : null,
  ]
} %}
